import 'package:flutter/material.dart';

class WeekToDosPage extends StatelessWidget {
  const WeekToDosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
